<div class="infobox-template">
  <div class="info-window" data-index="{{index}}">
    <h4>{{region}}</h4>
    <p>{{summary}}</p>
    {{#if url}}
      <a href="{{url}}" class="button ui primary">Read more</a>
    {{/if}}
    <div class="triangle"></div>
  </div>
</div>
